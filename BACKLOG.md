# Backlog

Review findings not immediately fixed. **Only work on these upon explicit request.** Process: `agent_docs/backlog_process.md`.

## Open

| # | Date | Category | Sev | Location | Finding | Status | Source |
|---|------|----------|-----|----------|---------|--------|--------|
| 1 | 2026-07-19 | Code Smells | P2 | settings_ui.py → SettingsWindow | ~1950-line file (grew with the diagnostics buttons 2026-07-20 and the status card / cancel buttons 2026-07-21), well over the ~500-line guideline; candidate to split into per-page modules (General/Whisper/Audio/Overlay/Integrations/Assistant/History/Help) | Deferred | Optimizer setup |
| 2 | 2026-07-20 | Edge Cases | P2 | transcriber_openvino.py → openvino_model_repo | A custom CTranslate2 HF model id typed while the OpenVINO backend is selected passes through verbatim, downloads the wrong-format model and fails only at pipeline load with a cryptic error. Documented in tooltips/hint; a format pre-check (probe for `openvino_model.xml` in the repo) would fail earlier and clearer | Accepted | OpenVINO backend #18 / PR #20 |
| 3 | 2026-07-20 | Security | P1 | updater.py → download_asset / netutil.py | With `insecure_ssl` on, the updater's exe download is unauthenticated (HTTPS host pinning doesn't help without cert verification) — a MITM proxy could serve a malicious binary that the app then swaps in. Hardening: verify the release asset's SHA256 `digest` from the GitHub Releases API after download, and/or keep `verify=True` specifically for the exe download even when `insecure_ssl` is on | Deferred (documented opt-in trade-off — owner decision) | Security audit 2026-07-20 |
| 4 | 2026-07-20 | Security | P1 | CLAUDE.md → Deployment "Routine exception" | Authorization is self-declared: any session whose kickoff prompt *claims* to be an owner-authorized routine gets pre-approved merge+deploy. A prompt-injection or malicious trigger text can claim this — there is no verifiable signal. Hardening: tie the exception to something checkable (e.g. a specific trigger id list) instead of self-declaration | Deferred (owner policy — needs owner review) | Security audit 2026-07-20 |
| 5 | 2026-07-20 | Security | P2 | .github/workflows/*.yml | Third-party actions pinned by major tag (`actions/checkout@v4`, `softprops/action-gh-release@v2`), not by commit SHA — a compromised tag would run in the release pipeline with `contents: write`. Hardening: pin by full SHA | Deferred | Security audit 2026-07-20 |
| 6 | 2026-07-20 | Edge Cases | P2 | settings_ui.py → _test_microphone/_test_transcription | The global hotkey stays active during a mic/transcription test — pressing it mid-test opens a second PortAudio input stream on the same device (both keep working in shared mode, but statuses interleave). The reverse is guarded (tests refuse to start while recording). Polish: pause the hotkey listener for the test duration, mirroring the hotkey test | Deferred | Settings self-test buttons (claude/settings-test-buttons) |
| 7 | 2026-07-20 | Edge Cases | P2 | settings_ui.py → _test_hotkey (idle guard) | The idle guard reads App.state, but a hotkey_press event can sit up to 100 ms in the event queue (poll QTimer) — hold the combo and click "Test hotkey" within one poll tick and the guard passes, the app listener is stopped, and the queued press starts a hold-mode recording whose release is then lost (stuck until overlay stop / max_seconds). Window ≤100 ms, recoverable; robust fix needs a "pending press" check or draining the queue before the guard | Deferred | Settings self-test buttons re-review 2026-07-20 |
| 8 | 2026-07-20 | Bugs & Logic | P2 | audio.py → Recorder.start | If `sd.InputStream(...)` constructs but `stream.start()` raises, `self._stream` stays assigned: `active` stays True and every later `start()` raises "recording already active" until app restart — the hotkey recording path is then dead (each attempt just notifies the error). Also the opened handle is never closed. Pre-existing, unlikely (device vanishing between open and start); fix: reset `self._stream = None` + close on a failed `start()` | Deferred (out of scope of the diagnostics feature; diagnostics.record_clip already guards itself via try/finally) | Settings self-test buttons re-review 2026-07-20 |
| 9 | 2026-07-21 | Edge Cases | P2 | livetype.py → LiveTyper._run / stable_prefix | Commit boundaries trust Whisper's segment end timestamps: an end stamped slightly early/late can duplicate or swallow a word at the live/final seam (the final pass re-transcribes from `committed_frames`). Mitigated by committing only text stable across two passes at segment boundaries (usually silence); a word-timestamp-based offset (`word_timestamps=True`) would be more precise at extra decode cost | Accepted | Live typing (claude/live-typing) |
| 10 | 2026-07-21 | Bugs & Logic | P2 | app.py → _process (live.join timeout) | If the live-typing worker is still alive after the 60 s join timeout (hung decode/injection), `_process` proceeds with its last committed state — should the worker resume afterwards it could type its `pending` a second time (duplicate words). Pathological only; a shared "handed over" flag between `_process` and `_flush_pending` would close it | Accepted | Live typing (claude/live-typing) |
| 11 | 2026-07-21 | Edge Cases | P2 | settings_ui.py → _cancel_diagnostic | Cancel detaches the worker (a blocking HF download can't be aborted); restarting a test within ~100 ms can briefly open a second PortAudio input stream (shared-mode OK, same class as #6), and re-running a cancelled model download starts a second snapshot_download of the same repo in parallel (hub-internally safe via temp files + atomic rename, worst case duplicate bandwidth). Cosmetic; a short re-enable delay after Cancel would remove both | Accepted | Settings status card (claude/settings-status-ux) |

## Done

| # | Date | Done | Category | Location | Finding |
|---|------|------|----------|----------|---------|
