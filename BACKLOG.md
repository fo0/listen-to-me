# Backlog

Review findings not immediately fixed. **Only work on these upon explicit request.** Process: `agent_docs/backlog_process.md`.

## Open

| #   | Date       | Category      | Sev | Location                                                    | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status   | Source                                                                          |
| --- | ---------- | ------------- | --- | ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| 1   | 2026-07-19 | Code Smells   | P2  | settings_ui.py → SettingsWindow                             | **4447**-line file as of 2026-09-09, well over the ~500-line guideline; candidate to split into per-page modules (General/Whisper/Audio/Overlay/Integrations/Assistant/History/Help — the Home page already lives in home_page.py). Grown steadily from 2701 lines on 2026-08-03; the largest single contributors were the #97 UX sweep (Assistant connection test, floating-icon position reset, assistant request timeout) and #110/#111/#112 (model-list filtering per backend, factory-reset button and dialog, download progress plumbing). Re-measured every sweep since: still valid, still the clearest split target, and still a multi-module refactor that only pays off when it blocks work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deferred | Optimizer setup                                                                 |
| 9   | 2026-07-21 | Edge Cases    | P2  | livetype.py → LiveTyper._run / stable_prefix                | Commit boundaries trust Whisper's segment end timestamps: an end stamped slightly early/late can duplicate or swallow a word at the live/final seam (the final pass re-transcribes from `committed_frames`). Mitigated by committing only text stable across two passes at segment boundaries (usually silence); a word-timestamp-based offset (`word_timestamps=True`) would be more precise at extra decode cost. Re-checked 2026-08-17, 2026-08-31 and 2026-09-02: unchanged — `LiveTyper._run` still commits via `stable_prefix` on segment ends and no call anywhere in `src/` passes `word_timestamps`; the precise fix stays a decode-cost trade-off that needs a real-model measurement, not a blind flip, and that measurement is out of reach for an agent run (the zero-cost + deterministic test constraints rule out loading a real model)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Accepted | Live typing (claude/live-typing)                                                |
| 12  | 2026-07-23 | Edge Cases    | P2  | singleinstance.py → acquire/start_server                    | Transitional mixed-version coexistence: a pre-mutex build (lock = port 52697 bind, no mutex) running while a new build starts is invisible to the new guard — the new instance takes the mutex, finds the port taken (old app holds it as its lock), logs a warning and runs alongside. Self-heals once the old build is gone (normal updater flow never hits it: old exe exits before the new one starts). Not fixable safely from the new side — exiting whenever the port is foreign would reintroduce the false "already running" refusal the mutex was introduced to fix. Re-checked 2026-08-17 against the live releases list: kept, not obsolete — the mutex first shipped in build 11 (v2026.07.23.11), and every pre-mutex build 1–10 is still published (v2026.07.20.1 … v2026.07.23.10; newest release v2026.08.13.26), so the builds this describes remain downloadable; retire the entry once they are gone. Re-checked 2026-08-31, 2026-09-02 and 2026-09-07 against the live releases list: unchanged — builds 1–10 (v2026.07.20.1 … v2026.07.23.10) are still published, newest release now v2026.09.06.32                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted | Single-instance fix (claude/app-instances-overlay-icon-nnv62z, PR #14)          |
| 16  | 2026-08-04 | Performance   | P2  | integrations.py → _tap / _activate / _undo                  | The keybind worker holds `_lock` across its whole key sequence, and `_tap` sleeps `_TAP_HOLD_S` (30 ms) per toggle target inside it — so a recording edge arriving on the Qt main thread can block on `_lock` for ~150 ms with all five presets enabled, or ~300 ms in the worst case where an activation undoes an overtaken stop first. The realistic trigger is `reset()` during shutdown while a recording is running (`App._quit` → `reset()` runs on the main thread), i.e. a brief hitch on quit; a start/stop pair landing inside the same window is not a real user scenario. Not fixed because the lock is what makes check-and-act atomic — a worker that passed its generation check must complete its sequence before the next edge touches the bookkeeping, and splitting it (separate send lock, or moving the sleep out) reintroduces exactly the interleaving the generation counter exists to prevent. Revisit only if the shutdown hitch is ever observed. Re-checked 2026-08-17, 2026-08-31 and 2026-09-02: unchanged — `_TAP_HOLD_S` is still 30 ms and `_tap` still sleeps it while the caller holds `_lock` via `_claim`; no hitch reported                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Deferred | Mute keybind review (claude/discord-mute-shortcut-conflict-vrlee3, PRs #56–#58) |
| 17  | 2026-08-09 | Edge Cases    | P2  | injector.py → insert/_type                                  | No upper bound on the injected payload length: in typing mode a runaway assistant response goes out as thousands of individual key events with no abort path, interleaving with whatever the user types meanwhile (live typing gates per word; paste is instant — only the classic typing path is exposed). Deferred: a cap or auto-switch-to-clipboard above a threshold changes behavior for legitimately long dictations and needs a product decision on the threshold and the fallback. Re-checked 2026-08-17, 2026-08-31 and 2026-09-02: unchanged — `Injector._type` still hands the whole sanitized string to a single `Controller().type()` call, while the live-typing path (`type_plain`) is the only one that goes word by word and can pause. The assistant side puts no ceiling on the payload either: `assistant.py` sends no `max_tokens`, so only the endpoint's own limit bounds a runaway response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Deferred | Full-codebase review (claude/bugs-smells-review-qlyapd)                         |
| 18  | 2026-08-09 | Code Smells   | P2  | transcriber\*.py                                            | The session-CPU-fallback scaffold (`_lock`/`_use_lock`, `_cpu_fallback_for` marker, recover-and-retry, cached-vs-downloading notify wording) now exists three times — CT2, OpenVINO, and since 2026-08-09 Parakeet (whose copy was added because the duplication had already cost it the inference-time fallback). Extract a shared base/mixin when the next fallback change lands; until then any fix must be applied to all three by hand. Re-checked 2026-08-17, 2026-08-31 and 2026-09-02: unchanged — `_use_lock` + `_cpu_fallback_for` still exist in all three transcribers, and their `_forced_cpu` keys still differ in shape (device + compute_type / openvino_device + openvino_precision / plain device), which is what makes this an extraction with a design decision in it rather than a lift. #110 added a fourth per-backend copy of the same shape: `transcriber.py` and `transcriber_parakeet.py` each own a `_download_watcher()` resolving their repo, cache folder and (Parakeet) file filter, while `transcriber_openvino.py` builds `progress.DownloadWatcher` inline in `_ensure_loaded_locked` instead. Those paths genuinely differ, so they belong in the same extraction, not in a separate one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Deferred | Full-codebase review (claude/bugs-smells-review-qlyapd)                         |
| 19  | 2026-08-09 | Edge Cases    | P2  | hotkeys.py → hold mode                                      | No recovery from an OS-swallowed release event: a UAC secure desktop or raw-input game eats the key-up, `_pressed`/`_active` stay stuck and the recording runs to the `max_seconds` cap (default 300 s) before auto-stop + notification end it. Fix would be a Windows-only `GetAsyncKeyState` poll of the combo's vks while `_active` (pattern exists in `injector.modifiers_down`); deferred because it is Windows-specific, needs real-hardware verification, and the cap already bounds the damage. Re-checked 2026-08-17, 2026-08-31 and 2026-09-02: unchanged — `Hotkeys` still has no key-state poll (no `GetAsyncKeyState` / `VkKeyScanW` anywhere in the module), cap still 300 s (`config.DEFAULTS["max_seconds"]`). Implementation attempted 2026-08-12 on explicit owner instruction and **discarded at the ~50-LoC autonomous-change budget: measured 79 insertions in `hotkeys.py`** (compiled clean, `python -m compileall` green). Design notes so the next attempt starts from them, not from scratch: the fix is the poll **and** a virtual-key mapping, not just one — `pynput` hands character keys over as `KeyCode(char=..., vk=None)`, so `user32.VkKeyScanW(char)` is the layout-aware route (low byte = vk, `-1` = the layout cannot produce it), and its result **must** be truncated to `ctypes.c_short` or ctypes' default int restype turns that `-1` into `65535`, whose low byte `0xFF` passes for a real vk; `Key` members (`Key.ctrl`, …) carry their vk on `key.value.vk`, not `key.vk`; mapping has to be all-or-nothing per combo, because polling a partial set could read "released" while an unmapped key is still held. One fact the 2026-09-02 re-check adds to those notes: the mapping half no longer has to be written — `integrations._char_vk()` already is exactly it, `VkKeyScanW` with an explicit `restype = ctypes.c_short`, the `-1` case and the low-byte extraction all handled, so a next attempt imports that helper instead of rebuilding it and starts well under the 79 insertions measured in 2026-08. That shrinks the budget problem but not the reason this stays deferred, which is the residual risk below. Residual risk that keeps this deferred even with the mapping solved: `GetAsyncKeyState` reports keys as up while a UAC secure desktop owns the input queue — i.e. the poll can stop a take **early** in the very scenario it targets, which is worse than today's late stop, and separating the two needs real Windows hardware no agent session here has | Deferred | Full-codebase review (claude/bugs-smells-review-qlyapd)                         |
| 22  | 2026-09-06 | Edge Cases    | P2  | injector.py → _paste (clipboard restore)                    | The previous clipboard is restored after a fixed 0.3 s; a target that services WM_PASTE later (RDP/Citrix, busy Electron app) reads the _restored_ content — the old clipboard text lands where the dictation should be, no log line. Fix: longer/adaptive window (≥ 0.5 s), log when the read-back shows the clipboard untouched, expose the delay next to `restore_clipboard`. Partly done 2026-09-07: the delay is the named constant `_PASTE_READ_WINDOW_S` at 0.5 s and a restore now logs the window it granted. What remains is exposing it in Settings next to `restore_clipboard` — a config key, a Settings row, `.env`-free but README-table and `agent_docs/key-patterns.md` visible, which is why it was not folded into the same change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Deferred | `claude/app-quality-model-optimization-7eel5p` (#168) quality pass, 2026-09-06  |
| 28  | 2026-09-06 | Feature       | P2  | transcriber_parakeet.py → MODEL_NAME / MODEL_REPO           | The Parakeet backend runs one hard-coded model. `nvidia/canary-180m-flash` (CC-BY-4.0, en/de/fr/es, German MLS 4.81 WER, ≈ 215 MB int8 via `istupakov/canary-180m-flash-onnx`) would be a low-footprint German+English option; needs a `parakeet_model` config key + dropdown, `recognize(wav, language=…)` plumbing (Canary takes no auto-detect), per-repo cache probe / download filter / status target. Not a default: Parakeet-TDT-0.6b-v3 stays faster and more accurate. Research: `docs/research/2026-09-stt-engine-delta.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deferred | `claude/app-quality-model-optimization-7eel5p` (#168) research, 2026-09-06      |
| 36  | 2026-09-08 | Edge Cases    | P2  | injector.py → _paste (clipboard restore)                    | With `restore_clipboard` on and a previous clipboard that pyperclip reads as "" (an image, a file list, or a genuinely empty clipboard), the transcript is only scrubbed when `clipboard_copy` is `off`. Under `on_failure` the paste succeeded, so nothing should stay behind, yet the dictated text keeps the clipboard and `_paste` reports it as kept — the branch comment calls `on_failure` a "keep-friendly mode", which is what the setting's own wording does not say. Fix is one condition, but it changes observable behaviour (the clipboard gets cleared where it previously did not), so it wants a decision rather than a sweep commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deferred | `claude/upbeat-davinci-v5r66z` best-practice sweep, 2026-09-08                  |
| 37  | 2026-09-09 | Accessibility | P2  | theme.py → QFrame#hero gradient                             | The Home page's hero card paints white text on the indigo→violet accent gradient, and its secondary line (`QLabel#heroHint`, `#eef1ff`) measures 3.80:1 on the first stop and 4.01:1 on the second — below the 4.5:1 WCAG minimum for normal text. The headline above it is 16 pt bold and passes as large text, so only the hint line is affected. Unlike the accent _fill_, which got a token of its own on 2026-09-09, this one cannot be fixed by a token swap: the gradient is the card, so clearing the threshold means darkening both stops (`ACCENT` / `ACCENT_ALT`) and restyling the app's most prominent surface — a design decision, not a sweep edit. A brighter hint colour is the cheaper half (pure white is 4.28:1 on the first stop and still short), so the gradient has to move either way.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Deferred | `claude/kind-lovelace-zfwxxn` UX best-practice sweep, 2026-09-09                |
| 38  | 2026-09-09 | UX            | P2  | settings_ui.py → diagnostic/test status labels              | The app defines `QLabel[role="error"]` (danger colour, semibold) precisely so that an inline failure cannot be mistaken for the help text beside it, but the only widget using it is the onboarding wizard's hotkey error. In the settings window all ten failure messages go through `_hint()` and render in the same muted grey as an explanatory note: "Microphone test failed: …", "Model download/load failed: …", "Transcription test failed: …", "Update check failed: …", "Test failed: …", "Cannot test — …", "Invalid hotkey — fix the combination first.", "Could not listen for the hotkey: …", "Could not open your browser", "Not found". The text names the failure, so this is a visual gap rather than a WCAG one. A fix is one helper that sets the role and repolishes, but it only holds if _every_ `setText` on those six labels routes through it — otherwise an error style survives into the next success message — and that is ~40 call sites across the file, well past what a sweep commit should touch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Deferred | `claude/kind-lovelace-zfwxxn` UX best-practice sweep, 2026-09-09                |
| 39  | 2026-09-10 | Code Smells   | P2  | overlay.py → Overlay · home_page.py → HomePage              | Both far past the ~500-line guideline: **overlay.py 1154** and **home_page.py 785** lines, measured 2026-09-10 (`wc -l src/listen_to_me/*.py`). overlay.py has climbed from ~1015 on 2026-08-31 and now carries the watchdog ladder, the monitor-anchored position, the context menu, the preview bubble, the progress ring and the source-aware labels of #191 — separable along those seams. home_page.py grew from ~519 with the second source's key-cap row, its caption logic and the "System audio hotkey" quick action. Both were assessed as "cohesive for now, revisit if it keeps climbing" in `agent_docs/refactoring_guidelines.md`; they have climbed. Same disposition as #1: the clearest split targets after settings_ui.py, and still only worth doing when a change to them is blocked by the size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Deferred | #190/#191 (`claude/intelligent-hawking-5p60x9`)                                 |
| 40  | 2026-09-10 | Code Smells   | P2  | app.py → App._handle                                        | **1852 lines** as of 2026-09-10 (from ~1420 on 2026-08-31). `agent_docs/refactoring_guidelines.md` says to extract the `_handle` event dispatch "only if it keeps growing" — #191 grew it: every recording branch now routes by `event_source(payload)` and checks `_take_source(self)` before it acts. Extracting the dispatch has one non-obvious constraint that must survive it: `App._handle`, `_register_hotkey` and `_toggle_hotkey_pause` are borrowed **unbound** by the headless self-test onto stubs that define only a handful of attributes, which is why the second source is read through module-level helpers (`_take_source`, `_notify_source_busy`) rather than App methods — a refactor that turns them into methods breaks those checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Deferred | #191 (`claude/intelligent-hawking-5p60x9`)                                      |
| 41  | 2026-09-10 | Edge Cases    | P2  | fillers.py → normalize · config.py → DEFAULT_FILLER_PHRASES | A symbol-wrapped filler annotation such as `♪ Musik ♪` is **deliberately not caught**. `normalize` trims `♪` as a symbol (Unicode `So`) and collapses the whitespace, so the transcript reduces to `musik` — while the shipped list only carries the bracketed forms `[Musik]` / `(Musik)`, which normalize with their brackets intact. Catching the symbol shape needs a bare `Musik` entry, and that would also swallow somebody dictating the single word "Musik" — the exact confusion the bracket rule exists to prevent. Recorded so the gap is a decision rather than an oversight: a user who sees this shape adds the phrase themselves, accepting that trade for their own list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted | #190 (`claude/intelligent-hawking-5p60x9`)                                      |
| 42  | 2026-09-10 | UX            | P2  | settings_ui.py → _assistant_values / _test_assistant        | **Test connection** on the Assistant page always tests the _microphone_ profile: `_assistant_values()` builds its request from `a_url_edit`/`a_model_edit`/`a_prompt_edit`, i.e. the Connection card plus the microphone card. The system-audio profile's **model override** and its prompt are never exercised, so a user who points meetings at a larger model gets a green test for a model the meeting path will not use — and an override naming a model the endpoint does not have fails for the first time after a 15-minute recording. A fix is a per-profile test (a second button, or a source selector next to the existing one) reusing `assistant.profile(values, source)`, which already resolves both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Deferred | #191 (`claude/intelligent-hawking-5p60x9`)                                      |
| 43  | 2026-09-10 | Code Smells   | P2  | CLAUDE.md → context budget                                  | **19,352 chars** after this pass (from 19,450; offload threshold 20,000, target 15,000) — so it is under the threshold but ~4,300 over target, and the next feature that needs a line in it has almost no room. The ladder in `agent_docs/context_budget.md` is largely spent: the spine it forbids offloading (Session Start, Workflow Triggers, Output Languages, Commands, Git Conventions, Documentation Rules, Caveman Mode, Autonomy, Handoff Prompt, Subagents) already measures ~11,700 chars, and every remaining section is at or below its rung allowance. Reaching 15,000 therefore means a deliberate pass that reduces most non-spine sections to bare pointers (rung 10 applied repeatedly) — a restructuring of the guide, not a side effect of a feature commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Deferred | Docs pass on `claude/intelligent-hawking-5p60x9`, 2026-09-10                    |

## Done

| #   | Date       | Done       | Category      | Location                                               | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ---------- | ---------- | ------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2   | 2026-07-20 | 2026-07-29 | Edge Cases    | transcriber_openvino.py → _ensure_loaded_locked        | A custom CTranslate2 HF model id entered while the OpenVINO backend is selected passed through verbatim, downloaded the wrong-format model and failed only at pipeline load with a cryptic error. Fixed: the first (downloading) load of a custom id lists the repo files via `_looks_like_openvino_model()` and refuses one without `openvino_*.xml`, naming both ways out; an undeterminable listing (offline, private repo, hub error) stays permissive, presets and cached models skip the check. gui_smoke covers the IR/CT2 directory cases                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 6   | 2026-07-20 | 2026-07-29 | Edge Cases    | settings_ui.py → _set_diag_busy                        | The global hotkey stayed active during a mic/transcription test — pressing it mid-test opened a second PortAudio input stream on the same device and interleaved the statuses. Fixed: `_set_diag_busy` pauses the app's listener for the tests that record (`tx`/`mic`) and gives it back on every done/fail/cancel path; a model download keeps the hotkey alive, and `_finish_hotkey_test` no longer re-registers over a running diagnostic. gui_smoke asserts the pause/resume                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 7   | 2026-07-20 | 2026-07-29 | Edge Cases    | settings_ui.py → _app_busy                             | The idle guards read App.state, but a hotkey_press event can sit up to 100 ms in the event queue (poll QTimer) — starting a test within one poll tick let the guard pass, stopped the app listener, and the queued press started a hold-mode recording whose release was then lost. Fixed: `_app_busy()` drains the queue via `App._poll()` before reading the state and is now the single guard for the hotkey, microphone and transcription tests. gui_smoke covers the queued-press case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 8   | 2026-07-20 | 2026-07-26 | Bugs & Logic  | audio.py → Recorder.start                              | A stream that constructed but failed to `start()` left `self._stream` assigned, so `active` stayed True and every later `start()` raised "recording already active" until restart (and the handle leaked). Fixed: the stream is published to `self._stream` only after a successful `start()`, and closed on failure. gui_smoke covers it ("recorder start failure resets")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 10  | 2026-07-21 | 2026-07-29 | Bugs & Logic  | app.py → _process (live.join timeout)                  | If the live-typing worker was still alive after the 60 s join timeout (hung decode/injection), `_process` proceeded with its last committed state — a worker resuming afterwards could type its `pending` a second time (duplicate words). Fixed: `LiveTyper.hand_over()` takes the remainder over under the same lock `_flush_pending()` holds, so the worker types nothing once `_process` owns it. gui_smoke covers the hand-over                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 11  | 2026-07-21 | 2026-07-29 | Edge Cases    | settings_ui.py → _cancel_diagnostic                    | Cancel detaches the worker (a blocking HF download can't be aborted); restarting a test within ~100 ms could briefly open a second PortAudio input stream, and re-running a cancelled model download started a second snapshot_download of the same repo in parallel. Fixed: `_begin_diag_cooldown()` keeps the start buttons disabled for 400 ms after a Cancel, via a QTimer child of the window (so it can't fire into a deleted one). gui_smoke covers the cool-down                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 13  | 2026-07-25 | 2026-07-26 | Edge Cases    | settings_ui.py / home_page.py                          | Code-painted pixmaps (nav glyphs, Home quick-action icons) and the Help browser's baked style sheet kept the old palette after a live OS light/dark switch. Fixed: `SettingsWindow._on_color_scheme_changed` rebuilds them (`HomePage.restyle_icons` + `_render_help`). The sidebar brand logo needs no rebuild — `icons.mic_image` uses fixed state colours, not theme tokens                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 14  | 2026-07-25 | 2026-07-26 | Performance   | app.py → _open_settings                                | Replaced SettingsWindow objects were never freed (signal-connection bookkeeping pins the wrapper), so every open/close/reopen cycle retained a whole window incl. its pixmaps. Fixed: `_open_settings` `deleteLater()`s the window it replaces, and `_set_state` treats a `RuntimeError` from the deleted wrapper as "gone"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 15  | 2026-07-30 | 2026-07-31 | Edge Cases    | settings_ui.py → _status_value / _hint                 | Same page-widening trap as the combo boxes, one widget class over: a wrapping `QLabel` reports the longest _word_ as its minimum width, and a Windows path or a repo id has nothing to wrap at (measured: 547 px for a realistic exe path, 866 px for a status line carrying one). Fixed: `qtutil.elastic_label()` caps what the layout asks for — an explicit minimum width replaces the longest-word minimum in `qSmartMinSize`, `QSizePolicy.Ignored` keeps the label out of the layout's preferred width, and the text stays untouched so a selectable label still copies the full path. Applied to every value on the Whisper status card and to the six hints composed at runtime from a probe, a path or an exception (hotkey / microphone / transcription test, autostart, model-cache hint, update status). gui_smoke measures the widget-item minimum of all eight against the elastic floor — the existing page-clipping guard could not catch this because it only ever renders short strings |
| 20  | 2026-08-09 | 2026-08-10 | Edge Cases    | app.py → _owns_take / _start_recording                 | `auto_stop` and `stream_died` are posted from PortAudio's callback thread and can sit up to 100 ms in the event queue, but carried no take id — an event from take A drained after a stop-A/start-B pair inside one poll tick would stop take B with A's message. Fixed: `_start_recording` reserves the next recording id before handing the callbacks to the recorder and passes it as the event payload, and `_owns_take()` gates both branches on the running take instead of the state alone (a `None` payload keeps the old state-only behaviour). No `Recorder` API change was needed — the callbacks are already App-side lambdas, which is what the entry had assumed to be expensive. gui_smoke covers it ("recorder events carry their take")                                                                                                                                                                                                                                                  |
| 23  | 2026-09-06 | 2026-09-07 | Edge Cases    | assistant.py → rewrite (HTTP)                          | `requests` `timeout` is per socket read, not wall-clock, and `response.json()` reads an unbounded body: an endpoint emitting one byte per 100 s never trips the 120 s timeout, the `process` thread never returns and `App.state` stays `processing` until restart. Fix: `stream=True`, total deadline of `acfg["timeout"]` across `iter_content`, cap the body (~1 MiB) → `AssistantError`; the existing `except` then inserts the raw transcript. Fixed: the response is streamed, read against a `time.monotonic()` deadline derived from the same clamped `timeout`, and capped at 1 MiB; both limits raise `AssistantError`, which app._process already turns into a notification plus the raw transcript. A body that is not JSON at all now names the assistant instead of surfacing urllib3's decode error. gui_smoke's faked `requests` covers the stream flag, the size cap and the non-JSON body                                                                                               |
| 24  | 2026-09-06 | 2026-09-07 | Performance   | livetype.py → _flush_pending / hand_over               | `_flush_pending` holds `_hand_lock` across `type_plain` (one SendInput/XTest round trip per character) while `hand_over()` waits on that lock from the Qt main thread (`_finish_recording`, `_cancel_recording`, `_quit`): a 200-char burst freezes the GUI for its duration, a blocked `SendInput` (UAC secure desktop) hangs the main thread and `_quit`. Fix: swap `pending` out under the lock, type outside it, merge the remainder back only if `not _handed_over`. Fixed: `_flush_pending` swaps `pending` out under `_hand_lock` and types outside it, merging the untyped remainder back only while the worker still owns the state. A hand_over() landing mid-burst therefore returns without that text on purpose — only the worker could say how much of it reached the keyboard, and typing it again is what append-only live typing must never do; `committed_text` still carries the whole chunk into the transcript, the history and the notification                                     |
| 25  | 2026-09-06 | 2026-09-09 | UX            | settings_ui.py → _apply_values / _offer_model_download | Apply/Save tooltips promised “apply immediately”, but a model/backend/precision change was only loaded at the next recording — the first dictation after Save could block for minutes on a download with no warning. Fixed: a saved change to `model`, `backend`, `openvino_precision` or `parakeet_quantization` asks `diagnostics.model_cache_status` (disk-only, never the network, never raises) and offers “Download it now?” when the model is missing; Yes shows the Engine page and starts the download machinery that already sat behind “Download / load model”. Save no longer closes the window while that download runs, because `done()` cancels every diagnostic on the way out. A preset with no conversion for the chosen backend reports `error` rather than a missing download and is left to the status card, which already names it                                                                                                                                                  |
| 26  | 2026-09-06 | 2026-09-09 | UX            | settings_ui.py → _refresh_hotkey_error / _validate     | The hotkey was validated only at Save through a modal, while the onboarding wizard validates the same value inline; `model_dir` was never validated at all. Fixed: an error-styled label (`role="error"`) under the hotkey field, updated on `textChanged` and mirrored onto the field's accessible description, with the modal kept as the gate — focus sits on Save when it refuses, and a label there reaches no screen reader. Parsing needs pynput, which a headless run cannot import, so that case leaves the label empty instead of calling a combination broken it could not read. `_validate` now creates `model_dir` (`mkdir(parents=True, exist_ok=True)`), which answers “a directory or creatable” in one call and is what the first download would do anyway; it runs last of the checks, being the only one with a side effect                                                                                                                                                            |
| 27  | 2026-09-06 | 2026-09-09 | UX            | choices.py / settings_ui.py → precision dropdowns      | Remainder of the 2026-09 UX review after #31/#32/#33: the OpenVINO precision and the Parakeet quantization showed bare values while the Whisper presets name their download size. Fixed: both lists carry a note in `MODEL_CHOICES`' shape, rendered by the new generic `choice_label` / `choice_labels` / `choice_value` — the config keeps storing the bare value, and a hand-edited one still shows unannotated. Parakeet is one fixed model, so its sizes are absolute (int8 ~0.7 GB, fp32 ~2.5 GB — the split `transcriber_parakeet._download_filter` already counts download progress against); OpenVINO converts whichever preset is selected, so an absolute size would be wrong there and only the ratio between its precisions is stated. Both combos got `elastic_combo` like the model combo — items this long otherwise widen the page                                                                                                                                                       |
| 31  | 2026-09-06 | 2026-09-07 | Accessibility | settings_ui.py → fixed pixel heights                   | `setFixedHeight(80)` / `setMaximumHeight(140)` on the text areas ignore font size / accessibility scaling; derive from `fontMetrics().lineSpacing() * n`. Fixed: `qtutil.text_rows_height()` derives the height from the widget's own `fontMetrics().lineSpacing()`, applied to the initial-prompt and text-replacement boxes (4 lines), the assistant system prompt (8) and the release list (7). Unchanged at 100 % scaling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 32  | 2026-09-06 | 2026-09-07 | UX            | settings_ui.py → SettingsWindow size                   | `resize(980, 720)` / minimum 840×600 is never clamped to `screen().availableGeometry()` — a 1366×768 laptop at 125 % has 614 logical px, the window extends under the taskbar. Clamp in `showEvent` (or `App._open_settings`). Fixed: `showEvent` runs `_clamp_to_screen()` once, lowering the height and the minimum height to `screen().availableGeometry()`. Height only — the pages sit in scroll areas, so a short window costs a scroll, while one narrower than the 840 the forms are laid out for clips them outright (the gui_smoke page-clipping guard shows it at 800 px). gui_smoke asserts the clamp against a deliberately oversized window                                                                                                                                                                                                                                                                                                                                                 |
| 33  | 2026-09-06 | 2026-09-07 | Performance   | settings_ui.py → \_load_devices                        | PortAudio enumeration runs synchronously during window construction (the Home page documents that it "can stall for hundreds of ms"); defer to the first Audio-page show like the hardware probe, preselecting "System default" meanwhile. Fixed: the dropdown starts with the "System default" placeholder and is filled on the first visit to the Audio page, mirroring the Engine page's hardware probe. `_selected_input_device()` answers from the config until then (normalised the way `input_device_from_label` does, since a None-default key is stored unvalidated), and the unsaved-changes snapshot follows the resolved value so opening the page alone does not look like an edit. gui_smoke asserts both                                                                                                                                                                                                                                                                                   |
| 34  | 2026-09-06 | 2026-09-07 | Code Smells   | integrations.py → hold-mode guard                      | Skipping a toggle mute target in hold mode (shared key / modifier) is a workaround: the complete fix filters `LLKHF_INJECTED` events in the hotkey hook and releases/re-presses the held modifiers around the tap — design note next to `_hold_mode_skip_reason`. Done: the design note now sits in the `_hold_mode_skip_reason` docstring — the Windows `LLKHF_INJECTED` filter through pynput's `win32_event_filter` armed only while a tap is going out, plus releasing and re-pressing the hotkey's held modifiers around the tap, and the note that XTest events carry no injection flag so X11 needs its own answer. The fix itself stays in #21: both halves are keyboard-hook behaviour on a real Windows desktop                                                                                                                                                                                                                                                                                 |
| 35  | 2026-09-08 | 2026-09-09 | Edge Cases    | config.py → atomic_write_json                          | The temp file was fsync'd before `os.replace`, but the parent directory never was, so on POSIX a power loss right after the rename could lose the directory entry and leave the previous `config.json` / `history.json` in place — integrity was never at stake (the old file is intact, never truncated), this was durability of the last write only. Fixed: `_sync_directory` opens the parent read-only and fsyncs it after the replace, outside the write's `try` so a failed flush never enters the temp-file cleanup path. Best effort by design: Windows, the primary target, exposes no directory handle to sync, so both failure paths only log at debug level                                                                                                                                                                                                                                                                                                                                   |
