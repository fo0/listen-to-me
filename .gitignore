# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/

# Build output
build/
dist/
*.spec
# The release job's PortAudio build tree — src/, build/ and out/ at the repo
# root, from "Build PortAudio with WASAPI loopback" or a local repro (#194)
/portaudio/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Secrets / credentials — never commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# App config may hold the optional assistant API key — never commit a real one
config.json

# Claude Code personal settings (local permission rules) — never commit
.claude/settings.local.json
